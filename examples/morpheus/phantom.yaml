# Generated by `phantom-api mock derive`.
#
# Read from 76 recorded exchanges. Every protocol, path
# and session rule below was observed in the recording, not guessed.

mock:
  name: morpheus

  listen:
    host: 127.0.0.1
    port: 8443
    tls: self-signed

  protocols:
    # 76 exchange(s), 76 operation(s)
    - pack: openapi
      paths: ["/api/**"]

  responders:
    - match: "*"
      responder: corpus
      corpus: ./corpus
      # What to do when a request is not in the corpus:
      # fault | synthesize | forward | not-found
      on_miss: fault

  control_plane:
    enabled: true
    bind: 127.0.0.1
